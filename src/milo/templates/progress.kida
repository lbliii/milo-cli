{%- set pct = [[0, (state.progress * 100) | int] | max, 100] | min -%}
{%- set filled = [[0, pct // 5] | max, 20] | min -%}
{{ state.label | default("Progress") | bold }}: {{ ("█" * filled ~ "░" * (20 - filled)) | green }} {{ pct }}%
