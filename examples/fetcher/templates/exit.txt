{% from "components/_defs.txt" import status_line %}
{% if state.status == "done" %}
{{ status_line("success", "Fetched " ~ (state.url | bold) ~ ": " ~ state.status_code ~ " (" ~ (state.content_length | filesizeformat) ~ ")") }}
{% elif state.status == "error" %}
{{ status_line("error", state.error_message) }}
{% else %}
{{ status_line("info", "Goodbye!") }}
{% endif %}
