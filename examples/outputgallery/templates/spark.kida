{% from "components/_defs.kida" import header %}
{{ header(spark.title, description=spark.subtitle) }}

{% for row in spark.series -%}
  {{ row.name | cell_fit(10) }} {{ row.spark }}  {{ row.now | cell_fit(6) }} {{ row.trend }}
{% endfor %}
