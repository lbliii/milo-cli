{% from "components/_defs.kida" import status_line, format_time %}
{{ status_line("success", "Stopwatch stopped at " ~ (format_time(state.elapsed) | bold | green) ~ (" with " ~ (state.laps | length) ~ " lap" ~ ("s" if state.laps | length != 1 else "") if state.laps else "") ~ ".") }}
