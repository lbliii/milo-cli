build:
  output_dir: "public"
  incremental: true

build_badge:
  enabled: true

assets:
  minify: true
  fingerprint: true

html_output:
  mode: "pretty"
  remove_comments: true
  collapse_blank_lines: true

health_check:
  enabled: true
  verbose: false

health:
  linkcheck:
    exclude_domain:
      - "localhost"
    ignore_status:
      - "403"
