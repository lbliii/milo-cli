# Integration tests

Integration tests cross a process, protocol, package, or repository boundary.
They use explicit timeouts, capture stdout/stderr, and keep external versions
or source identities pinned.
