Add a dependency-free `CLI.asgi_app()` for modern MCP Streamable HTTP, an optional Uvicorn-backed `--mcp-http` mode, strict mirrored-header and Origin validation, bearer-token/RFC 9728 hooks, request-scoped SSE progress, HTTP verifier lanes, and free-threaded parallel tool-call proof. Legacy 2025-11-25 remains available over stdio.
